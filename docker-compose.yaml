version: '3.8'
services:
  csv-to-postgres:
    build:
      context: .
      target: production
    image: csv-to-postgres
    container_name: csv-to-postgres
    volumes:
      - ./input:/home/dotnet/input
      - ./output:/home/dotnet/output
